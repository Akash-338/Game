# Project TODO

- [x] Confirm the final visual design direction before implementation.
- [x] Build the real-time Word Impostor game using JavaScript and JSX only; do not author application features in TypeScript.
- [x] Create mobile player login with User ID, Room ID, password, persistent random avatar, ready state, and connection status.
- [x] Create the laptop host dashboard with room controls, player kicking, impostor controls, drag-and-drop turn ordering, hidden peeking mode, and post-round actions.
- [x] Implement local JSON word packs, category filters with Select All, custom JSON upload, random selection, and session-level duplicate-word prevention.
- [x] Implement real-time game state synchronization, envelope role reveal, hint board, host-only hint editing, current-turn notification, spectator mode, and manual reveal.
- [x] Add responsive custom CSS styling, haptic feedback, and sound controls; Tailwind was optional and is intentionally not part of the shipped JavaScript and JSX stack.
- [x] Write and run JavaScript-compatible automated tests and provide laptop tunneling instructions.
- [x] Do not begin implementation until the user explicitly says: "start building the entire project."
- [x] Define and document the game pages, responsive layouts, navigation, and visual design system before implementation.
- [x] Use the modern Warm Party Table visual direction with responsive layouts and purposeful motion design throughout the game.
- [x] Add the Host1234 entry rule: direct Host1234 to the host dashboard; require Room ID and password for all other User IDs.
- [x] Produce requirements.txt documenting the full requirements, page layouts, visual system, database schema, JavaScript and JSX framework, real-time architecture, and non-implementation constraint.
- [x] Produce a visual architecture and game-flow diagram before implementation.
- [x] Copy the planning requirements and diagram artifacts to C:\Users\aksh0\Desktop\Word_hunt without implementing the game.
- [x] Review the laptop-copied requirements document, Mermaid diagram source, and rendered diagram before coding.
- [x] Convert the JavaScript project foundation from TypeScript without leaving application TypeScript feature files.
- [x] Build the Host1234 entry path and room-password player/ghost join paths.
- [x] Persist rooms, players, rounds, hints, used word entries, word-pack metadata, and host alerts in SQLite.
- [x] Add local default word-pack JSON, validated custom JSON upload, category filtering, explicit word choice, random selection, and no-repeat enforcement.
- [x] Add host roster management: live presence, ready gating, kick, adjustable impostor count, drag-and-drop turn ordering, and private peeking.
- [x] Add random session avatars, envelope role privacy, role visibility toggles, haptics, sounds, and host alerts/nudges.
- [x] Add player and ghost real-time views, hint submission locking, host-only hint editing, turn progression, reveal, and Continue/Restart flows.
- [x] Build and test Socket.IO event authorization and per-audience data visibility.
- [x] Deliver a responsive Warm Party Table UI with required motion and reduced-motion support.
- [x] Test backend logic, live game flow, responsive phone and laptop layouts, and local tunneling instructions.
- [x] Copy the finished JavaScript and JSX project source to C:\Users\aksh0\Desktop\Word_hunt for local VS Code use.
- [x] Complete an explicit keyboard, focus, semantic-label, and reduced-motion accessibility verification pass.
- [x] Perform and document visual QA for lobby, live-game, and results at both phone and laptop widths.
- [x] Exercise the actual laptop-hosted tunnel and phone-style join flow, or document any environment blocker precisely.
- [x] Safely remove temporary archives, generated build output, logs, and cache files from the local laptop project while preserving source code, documentation, word packs, and game data.
- [x] Inspect and safely clean generated artifacts inside the copied local app folder, then verify the retained source, documentation, word-pack, and game-data structure.
- [x] Inspect the copied local app for remaining logs and temporary files, then explicitly verify the word-pack and game-data locations are preserved. No log or temporary files were present; `server/data/default-word-pack.json` remains, and no local game-session data has been generated yet.
- [x] Remove pnpm-specific and YAML configuration files from the local laptop project and update all local instructions to npm commands.
- [x] Audit the entire copied local project for remaining pnpm references and YAML configuration files; no local YAML files or pnpm references remain.
- [x] Validate the copied local project with npm install, npm test, and npm run build after the npm-only conversion.
- [x] Align the tracked project package configuration and local-hosting documentation with the verified npm-only local copy.
- [x] Restore only the sandbox-managed package configuration needed for the development service, without reintroducing YAML or pnpm files into the user's local laptop copy.
- [x] Add and verify a JavaScript-compatible managed health-check command in both the tracked and local npm project manifests.
- [x] Perform a final safe cleanup of nonessential files from the local npm project while retaining all files required to run the game.
- [x] Remove any remaining `.project-config.json`, `components.json`, `template.json`, `.prettierrc`, and `.prettierignore` files from the local Windows npm project.
- [x] Move the local Word Impostor runtime and all documentation from port 3000 to an available nonreserved port, excluding 5000, 5173, 5174, and 3306.
- [x] Audit every local Markdown file and update any remaining runtime or tunneling reference from port 3000 to port 4300.
- [x] Expand the local default word pack with new original categories, secret words, and concise impostor hints.
- [x] Replace every local default-word-pack impostor hint with a single-word clue.
- [x] Reorganize the default word pack into Animal, Fruits, Food, Object, Karnataka Big Cities, and Sports categories in that order, using numeric entry IDs and compatible game validation.
- [x] Normalize numeric word-entry IDs in the host selection interface so selected-word preview, round start, and no-repeat tracking remain correct.
- [x] Mirror the ordered numeric-ID word-pack changes into the tracked project and add permanent regression coverage for selected-word preview and no-repeat filtering.
- [x] Add more default word-pack entries, keep each category grouped in order, and assign every new entry the next sequential numeric ID.
- [x] Restore npm dependencies in the renamed local Windows project and verify port 4300 is reachable before retrying the VS Code tunnel.
- [x] Diagnose and resolve tunnel-related API, Vite WebSocket, and favicon console errors without changing core game logic.
- [x] Verify the public Dev Tunnel reaches the revised production game entry screen after its one-time trust notice.
- [x] Verify that the public Dev Tunnel accepts the Socket.IO WebSocket upgrade used for real-time game updates.
- [x] When the host exits, end the room gracefully after a 10-second reconnect grace period and notify remaining players instead of causing a critical client error.
- [x] Persist submitted hints immediately, automatically advance the turn, and retain distinct hints for each player across multiple rounds.
- [x] Permanently delete terminated rooms, passwords, players, rounds, hints, alerts, and used-word records so the same Room ID can be reused.
- [x] Replace the partially visible envelope role reveal with a fully opaque open/close control that conceals the player’s word until intentionally revealed.
- [x] Diagnose the local Windows port-4300 conflict and verify the game server remains available for phone access.
- [x] Identify the Windows process occupying port 4300 and confirm it is the Word Impostor server (`node server/index.js`, PID 18176).
- [x] Re-verify the active public tunnel reaches the local game server and document the correct startup guidance; the current tunnel returns the Word Impostor entry page.
- [x] Diagnose and resolve the active ML01 room-access failure for the local laptop game server; ML01 and its supplied password were verified, and Host1234 recovery was added for existing-room management.
- [x] Let Host1234 reclaim an existing room through password-verified Room ID entry after a refresh or new host login.
- [x] Verify Host1234 recovery into an existing local room end-to-end; ML01 was already purged after its host session ended, so it cannot be reclaimed. The public tunnel is active and presents its standard trusted-tunnel notice; the isolated Socket.IO recovery check passed.
- [x] Make the player interface explicitly direct late arrivals to Ghost mode when a room is already playing.
- [x] Align the local default word-pack metadata with the tracked integer-version format so local regressions can run.
- [x] Allow a player who leaves an active room to return with the same User ID and room password, preserving their place in the game.
- [x] Retain and regression-test complete room-session purge when the host leaves, including players, room credentials, rounds, hints, alerts, and used words.
- [x] Confirm every submitted hint is included in the shared real-time player and Ghost hint board.
- [x] Remove the host secret word and category from the main host dashboard during play so nearby players cannot see it.
- [x] Restyle the shared player hint history with a neutral surface and consistently dark, readable text that matches the surrounding interface.
- [x] Capture and fix verified browser console, network, and local server warnings for the phone-hosted game; the live public tunnel entry page, interactive entry form, refreshed page, and local production server all show no browser-console, network, or runtime errors.
- [x] Exercise the live tunnel form using the actual input and button, then confirm the browser console remains clean after navigation; the live Host1234 entry form successfully navigated to the host room controls with no console output.
- [x] Inspect live public-tunnel network requests during loading and form interaction for failed assets, socket, or API calls; the document returned HTTP 200, web-font files returned HTTP 200, and no failed game assets or API calls were recorded.
- [x] Explicitly scope non-browser development advisories: the Node SQLite experimental notice is emitted only by the sandbox development runtime, and Vite’s bundle-size recommendation is a build-time optimization advisory; neither is emitted in browser DevTools or affects the live tunnel game flow.
- [x] Exercise a live Socket.IO room action through the tunnel and confirm that no WebSocket or API request fails; an isolated DIAG91 room was successfully created through the public host controls with no browser-console errors, then ended and purged successfully.
- [x] Capture and inspect the actual Socket.IO transport traffic for a live create-and-end diagnostic room action before certifying the real-time network path as clean; the public tunnel used the WebSocket transport and returned successful createRoom and endRoom acknowledgements for an isolated NET82411 room.
- [x] Verify that cross-origin web-font timing entries are benign rather than failed asset loads; `document.fonts` reports DM Sans and Fraunces as loaded, so the status-0 CSS timing entry is a normal cross-origin timing limitation.
- [x] Run a complete isolated game test in room 9999 with password 9999, six players, and two impostors.
- [x] Exercise joining, readiness, role assignment, turn order, multi-round shared hints, same-name rejoin, reveal, and host exit during the room-9999 test.
- [x] Fix and retest any verified defect found during the full room-9999 scenario; the test setup sequence was corrected, and no production game defect was found.
- [x] Repair the managed deployment startup failure caused by the missing `dist/index.js` entry point.
- [x] End room 9999 after testing and verify permanent deletion of the room, password, players, rounds, hints, alerts, and used-word records.
- [x] Keep the Windows laptop workflow in development mode and stop the existing port-4300 listener before starting a single development server.
- [x] Stop the detached local development server so the user can run a terminal-owned `npm run dev` process that ends when its terminal closes.
- [x] Verify that `npm run dev` starts no detached process and remains tied to the terminal lifecycle, preserving the current launcher because it is already compliant.
- [x] Increase the accidental host-disconnect recovery grace period from 10 seconds to 20 seconds while keeping the explicit End/Terminate Room action permanently immediate.
- [x] Show remaining players a clear live-game state such as "Waiting for the Host to Join" during the 20-second accidental host-disconnect recovery period.
- [x] On server startup, detect any unfinished room from a previous server process or crash and permanently purge its complete session data before allowing further play or Room ID reuse.
- [x] Prevent any normalized hint from being submitted more than once anywhere in the same room session, with a clear player-facing explanation when a duplicate clue is rejected.
- [x] Add a project-appropriate `.gitignore` that excludes generated dependencies, builds, local session data, logs, and environment secrets while retaining source and word-pack fixtures.
- [x] Synchronize the current core game source and `TODO.md` from the sandbox project to the Windows laptop copy.
- [x] Recheck browser console, network requests, runtime logs, tests, and builds; report any verified remaining issue accurately. Local Windows runtime logs and HTTP checks were clean; the same synchronized code had no console output and returned HTTP 200 in the sandbox browser. The public tunnel itself remains GitHub-authentication gated for this browser session.
- [x] Make the permanent game-flow regression isolate or reset its local test database state so prior Windows game records cannot affect host-recovery assertions.
- [x] Replace the shared scrolling hint history with responsive player-specific hint cards that retain each player’s clue history in one visible card.
- [x] Render each player-card header as the player name plus an accessible connected/offline status indicator, followed by numbered clue rows containing the round number and actual hint.
- [x] Blur and desaturate an offline player’s existing hint card without removing its history, then restore the normal card immediately when that same player rejoins.
- [x] Preserve current clue visibility and permissions: all players and Ghosts can read every card, only existing host edit controls remain available, and player cards do not gain edit controls.
- [x] Pass the complete player roster to the host hint board so cards and presence headers are visible before any clue is submitted.
- [x] Add regression coverage for zero-clue players with mixed connected and offline status in the host card model.
- [x] Restore the required host-screen privacy in the active round by withholding the secret word and category until the reveal stage.
- [x] Add a room-wide reconnect notice when a player reclaims their slot, while keeping the existing connection-status cards accurate.
- [x] Add a host-configurable clue-cycle limit that defaults to two complete clue cycles before voting becomes available.
- [x] Design and implement a private, simultaneous voting stage after the clue phase, including a live waiting state until all eligible players vote.
- [x] Move voted-out active players to Ghost mode while preserving their player card, clue history, score record, and read-only game access.
- [x] Define and implement deterministic handling for two-way and three-or-more-way vote ties, including player-facing notifications.
- [x] Evaluate civilians-versus-impostors win conditions after each elimination and declare the winning side without exposing unrevealed roles prematurely.
- [x] Add a transparent per-game score ledger, public activity log, and host score view for all current players and Ghosts.
- [x] Add an end-of-round recap indicator for strongest clues using a defined, non-fabricated rule.
- [x] Back up the current default word-pack data, split it into category-based JSON source files, and retain the existing combined runtime pack format.
- [x] Design a host word-pack manager with named packs, category/entry previews, enable-disable controls, and safe deletion rules.
- [x] Cap every voting result at a maximum of two eliminations, including ties, before applying Ghost-mode changes and victory checks.
- [x] Define a safe resolution for three-or-more-way top-vote ties that does not automatically eliminate all tied players.
- [x] Open the private voting window automatically after the last required clue of the current clue cycle, using two cycles before the first vote and one cycle before later votes.
- [x] Add an always-available room discussion panel that displays player name, time, and public deduction messages without exposing private role information.
- [x] Define and test server-authoritative voting edge conditions, including no-survivor draws, reconnects during ballots, abstentions, host recovery, and the two-elimination cap.
- [x] Implement the approved private re-vote among only the three-or-more players tied for the highest first-ballot total.
- [x] Add an authoritative room phase machine for clue cycles, ballot, runoff, post-tally continuation, final recap, and no-survivor draw outcomes.
- [x] Persist private ballots, public game events, public discussion messages, and per-game scores with complete session-purge coverage.
- [x] Build the private player ballot, public ballot-progress state, runoff interface, and real-time tally/elimination notifications.
- [x] Build the always-available public discussion interface with sender labels, timestamps, rate limiting, and host removal controls.
- [x] Build the game-log, score-ledger, final recap, and host-selected memorable-clue controls without exposing live private role data.
- [x] Back up and split the default word pack by category, then build the compatible combined runtime pack and host management controls.
- [x] Add end-to-end voting, runoff, scoring, discussion, reconnect, Ghost, privacy, purge, and Windows regression coverage.
- [x] Add a visible host lobby control for the clue-cycle limit and verify the selected limit controls first-ballot timing.
- [x] Show persisted memorable-clue awards in player and Ghost recap cards, not only in the host recap controls.
- [x] Add automated coverage for no-survivor draws, offline-voter abstentions, player reconnects during a ballot, and host recovery during voting.
- [x] Synchronize the voting, discussion, score, recap, and category-pack extension to Windows and run the full Windows validation suite.
- [x] Replace private ballot progress with a live public vote feed that identifies both the voter and selected target in real time for every active voting stage.
- [x] Enforce a server-authoritative 15-second cooldown between public discussion messages for each active player.
- [x] Add a host discussion-lock control that prevents new messages during the final voting period while keeping the existing history visible.
- [x] Attach explicit, player-visible reasons to every score change in the score ledger and public activity log.
- [x] Keep the public discussion chat panel visible and usable throughout clue play, voting, and re-voting, subject only to the existing host message lock.
- [x] Prevent active players from voting for themselves in both initial ballots and tied-player re-votes, using server validation and hidden client targets.
- [x] Create a complete, fully editable Excalidraw architecture diagram covering every Word Impostor feature, flow, data store, real-time path, privacy rule, recovery behavior, and regression suite without overlapping connectors.
- [x] Add consistent accessible Back controls to all non-final Word Impostor views where a player, Ghost, or host needs a safe return path, without abandoning an active session.
- [x] Reposition the Table Activity and discussion control with responsive safe-area spacing so it never overlaps the I’m ready or clue-submit actions.
- [x] Replace the floating Table Activity label with a compact top-bar chat icon beside the profile controls, close it on outside interaction, and ensure host start-game controls remain fully usable.
- [x] Remove duplicate React key warnings from the host dashboard when word packs contain repeated numeric IDs.
- [x] Position the compact chat icon immediately left of the profile avatar and keep it spatially separate from the exit button at every responsive width.
- [x] Allow a host-kicked player to join the same room again with the Room ID and password, using the room’s existing active-session rules.
- [x] Preserve pack-scoped used-word identities so multi-pack rooms cannot select an already-used word entry after continuing a round.
- [x] Audit every implemented feature, open checklist item, and regression path; complete any verified remaining partial work. No pending feature tasks remained; removed the stale Windows-only `client/src/todo.md` duplicate and completed full regression verification.
- [x] Set and verify the default game cadence: two complete opening clue cycles before the first vote, then one clue cycle followed by voting after every later round until a win condition is reached.
- [x] Replace random player avatar pictures with the first two letters of each player’s User ID across all game views.
- [x] Close the compact room-activity chat when the user scrolls, clicks or taps outside it, or shifts focus to another screen control.
- [x] Remove only confirmed redundant files, duplicate sources, and obsolete build artifacts while preserving required runtime, test, data, and deployment files. Removed the unreferenced template metadata files and the obsolete legacy room-activity implementation while retaining the active runtime, test, data, deployment, and package files.
- [x] Split oversized application code into focused feature modules with stable imports and no gameplay behavior changes. App.jsx now contains only real-time state, session routing, and feedback orchestration; entry, host, gameplay, results, privacy vault, voting, hint board, room activity, decor, and feedback interfaces live in focused modules.
- [x] Fix completed vote resolution so selected players are eliminated, moved to Ghost status, and publicly identified as impostor or civilian before the next game phase.
- [x] Audit all active client, server, Socket.IO, voting, privacy, recovery, browser-console, build, and Windows-local flows; fix every reproducible defect and document any non-defect advisories separately. Confirmed and corrected the shared role-vault privacy regression, reactive connection badge, obsolete UI audit, stale local port references, and QA defaults; all sandbox and Windows regression suites, flow checks, UI audit, and production builds passed.
- [x] Restore the established private-role reveal contract after modularization so the sealed vault never exposes data and an impostor receives only the intended category-or-hint detail, not both. The client model now delegates to the existing shared privacy model, with focused assertions for sealed and revealed states.
- [x] Repair the advertised UI-audit command so it validates the modularized active view files and current accessibility labels rather than obsolete pre-modularization paths. The command now checks the extracted views, shared top bar, current player clue label, focus style, responsive rules, reduced-motion rules, and reactive socket-status subscription.
- [x] Make the TopBar live/offline connection badge react immediately to Socket.IO connect and disconnect events. Added a lifecycle subscription with cleanup and regression coverage for initial state, connect, disconnect, and listener disposal.
- [x] Correct all remaining Windows local-hosting and tunneling documentation references from port 3000 to port 4300. The local startup guide now names `Word_Imposter` and port 4300 throughout; the verification notes now use the same current location and forwarding port.
- [x] Update manual local quality-assurance scripts to default to the Windows game port 4300 while retaining a GAME_URL override for managed preview testing. The standalone real-time, visual-state, and reveal QA scripts now default to 4300 and still accept GAME_URL for managed preview environments.
- [x] Keep the compact room-activity chat closed for every participant when a host starts a new game; it now closes at the lobby-to-playing transition and no longer reopens from game-phase updates. Participants can open it only through the top-bar activity icon.
- [x] Keep the room-activity panel open while a participant scrolls content inside Discussion, Scores, or Game log; dismiss only when the page itself scrolls or an eligible outside interaction occurs. The panel now distinguishes nested list scrolling from document scrolling, with focused regression coverage.
- [x] Eliminate every remaining automatic room-activity opening path so the panel opens solely from a deliberate top-bar activity-icon action. Removed phase-pinned discussion rendering and added a UI-audit assertion that rejects automatic panel-opening code.
- [x] Show every participant a clear public vote-resolution card after each completed ballot, including all eliminated names and whether each was an impostor or civilian. Added a snapshot-backed resolution card in active play and final results, so it remains visible even if a real-time notice races a snapshot update.
- [x] Show the complete impostor-name list on the final player result screen and ensure the final-reveal heading never renders without those names. Revealed role lists now include eliminated non-kicked players, and the final heading uses a non-misleading loading state until the authoritative names arrive.
- [x] Make impostor assignment session-fair: avoid immediately reselecting recently assigned impostors whenever enough eligible active players are available, while retaining unbiased randomness among eligible candidates. Added a random shuffle helper that excludes last-round impostors first and falls back only when necessary.
- [x] Trace and remove all remaining programmatic room-activity opening paths after game starts, voting resolutions, runoff resolutions, and final reveals. The panel now closes on every authoritative room status or game-phase transition and does not open from any phase handler.
- [x] Restore every former Ghost to the active player table when the host chooses Continue with the same players, before the next round’s role assignment and readiness flow. Continue now reactivates every non-kicked participant, clears readiness and roles, and the permanent flow check verifies a Ghost can ready and join the next round.
- [x] Make voting-table initials and round badges fixed, circular, and non-stretched on both laptop and phone layouts; keep vote rows readable without horizontal overflow. Vote avatars now use fixed square flex bases and aspect ratios, player names can wrap safely, and phone live-vote rows place timestamps beneath the message rather than widening the row.
- [x] Run a fresh end-to-end defect audit across active client code, Socket.IO flows, authoritative game service, browser logs, responsive layouts, production builds, and the synchronized Windows copy; repair every reproducible issue found. No new reproducible functional defects were found. All 34 regression tests, game-flow and recovery checks, UI audits, production builds, current browser/network logs, and Windows validations passed. The only advisories are Node’s experimental SQLite notice and a non-blocking sandbox production bundle-size warning.
- [x] Audit the repository and remove only confirmed unreferenced template artifacts, stale generated output, duplicate files, and obsolete one-off scripts; preserve all active runtime, test, tunnel, documentation, data, and development assets. Removed empty placeholder files, an unreferenced Wouter patch, and six unreferenced one-off architecture, data-split, and visual-QA scripts. Kept all runtime, test, data, tunnel, word-pack, and documented developer workflows.
- [x] Correct the preserved README local startup references to port 4300 so codebase cleanup does not leave misleading laptop and tunnel guidance.
- [x] Audit laptop and phone layouts across entry, lobby, clue, voting, runoff, Ghost, host, activity-panel, and final-result states; repair every verified overlap, clipping, unsafe fixed-control, spacing, or touch-target defect. Verified desktop and 375px phone entry renders, inspected state-specific responsive rules, corrected the discovered host-tab and internal-scroll defects, and documented the findings in `docs/visual-audit-notes.md`.
- [x] Make the host’s four activity tabs fit evenly instead of leaving the Packs tab on a partial second row. The responsive auto-fit grid now gives every tab an equal-width slot at available panel widths.
- [x] Ensure long Discussion, Scores, and Game log content scrolls within the activity panel rather than being clipped by the fixed panel height. Each content list is now a flex-safe, contained touch-scroll region inside the bounded panel.
- [x] Repair the active HostDashboard shared-word helper import revealed by visual QA, then re-check the host layout at laptop and phone widths. The corrected module now loads after development-server restart and production build.
- [x] Verify whether `default-word-pack.json` and `default-word-pack.before-category-split.json` are still required by the active loader, tests, or documentation; remove each file only if the category files fully replace it. Replaced the active combined-file loader with a stable virtual `warm-party-classic` pack assembled from the six category files, preserving pack-scoped history for existing rooms. Both combined files can now be removed safely.
- [x] Correct retained historical plan and architecture references that still describe the removed combined word pack as active storage. The plan and editable architecture diagram now identify `server/data/categories/*.json` as the built-in source.
- [x] Register each built-in category JSON file as its own `word_packs` database record, remove all combined default-pack rows, and preserve category selection plus no-repeat behavior for new rooms. Bootstrap registers six files individually, migrates legacy selections, rounds, and used-word keys to category pack IDs, then retires the obsolete combined rows.
- [x] Repair the active HostDashboard shared-model import path reported by the development server, then confirm the dashboard renders without an import-resolution error. The active dashboard uses the correct project-root shared helper path; the production build and restarted development service resolve it successfully.
- [x] Add an explicit player-initiated Leave Room flow that removes the player’s roster record immediately for every participant and the host, while preserving temporary disconnect/reconnect behavior when the player does not choose Leave Room. The active roster removes the intentional leaver immediately, records the departure in the room log, reconciles clue/vote state safely, and preserves normal reconnect behavior for passive disconnects.
- [x] Correct the reproduced HostDashboard shared-model resolution error and revalidate the current development service after the intentional-leave update. The active shared-model path resolves after restart; the production build and entry-screen render pass cleanly.
- [x] Redesign the editable Word Impostor Excalidraw architecture diagram using the supplied compact reference style, with small readable feature clusters rather than large boxes or table-like layouts. Rebuilt as 34 compact editable elements, validated as JSON in sandbox and Windows, and synchronized to the laptop project copy.
- [x] Locate the active Windows project directory and place the updated compact Excalidraw architecture diagram in its expected local folder after the user reported it missing. The confirmed project is `C:\Users\aksh0\Desktop\Word_hunt\Word_Imposter`; matching valid copies now exist at both its root and `docs` folder.
- [x] Expand the compact editable Excalidraw architecture diagram into a complete parent-to-child feature map covering every implemented Word Impostor feature, server flow, data record, safety rule, and regression path. The complete map contains 176 editable elements, six parent branches, 72 compact child feature cards, and validated matching copies at the Windows project root and `docs` folder.
- [x] Prepare a clean public-repository configuration: ignore platform-managed, local-only, generated, log, data, cache, credential, and editor artifacts; remove nonessential platform blueprint files and wording; preserve the standalone game source and document what is excluded. Added safe ignore coverage, deleted the obsolete platform debug collector and pnpm lockfile, removed platform wording from public docs, and passed sandbox plus Windows npm tests and builds.
- [x] Refresh `requirements.txt` with the complete current game requirements, validated architecture, Windows hosting instructions, category-pack model, testing expectations, and public-repository boundaries; then synchronize it to the local Windows project folder. Replaced the obsolete planning document with a 223-line implemented specification and verified the matching Windows copy by SHA-256 hash and required sections.
- [x] Audit all test files and test-only scripts; remove only confirmed redundant or obsolete artifacts while retaining coverage for game rules, role privacy, voting, reconnection, public UI behaviour, and build-critical flows. Removed the unused non-discovered `server/game/service.integration.js`; retained all 17 active test files and authoritative flow checks. Sandbox and Windows tests plus production builds passed.
- [x] Investigate the reported Continue with same players issue: ensure every eligible Ghost is authoritatively restored to an active player—not only displayed as active in the host roster—then fix and validate the full lobby-to-next-round flow if required. Fixed a real Socket.IO session-mode defect: Continue now clears connected players’ stale Ghost session flags, updates the saved player identity from the authoritative roster, and preserves the database restoration. Added focused regressions; sandbox and Windows tests/builds pass.
- [x] Prepare a detailed implementation plan for scaling The Word Impostor to thousands of concurrent users, covering a separated frontend/backend structure, managed PostgreSQL, Redis-backed realtime coordination, autoscaling deployment, security, observability, load testing, and rollout criteria. Created `docs/thousands-user-implementation-plan.md` with target architecture, repository migration, transaction/locking rules, realtime protocol, security controls, deployment topology, validation gates, and phased rollout; verified and synchronized it to Windows by SHA-256 hash.
- [ ] Guide and execute a controlled scalable deployment: confirm providers and accounts; restructure the project; provision PostgreSQL, Redis, and object storage; configure secrets; deploy frontend and realtime backend; verify gameplay; and document operating procedures.
- [x] Update word-reuse policy so a word cannot repeat within the same round but the usable-word pool resets only as intended for a new game/session, with the exact Continue behaviour documented before implementation. Continue now clears the prior word history only after the revealed round ends, so a prior word can be chosen again in the next round while the active round remains immutable.
- [x] Redesign score presentation so scores appear in a dedicated end-of-round results layout rather than the room-activity chat panel, while retaining clear score-reason history elsewhere as appropriate. Added the responsive Round Scoreboard to player and host results, showing placement, per-round change, and cumulative total; removed score display from the activity panel.
- [x] Preserve cumulative player scores across every round in a room until the host ends the game, with an explicit full-session reset policy. Continue no longer deletes score records or score events; End Room/Restart still purge the room ledger. Permanent flow coverage verifies both score carry-forward and word-pool reset.
- [x] Add private one-to-one direct messaging alongside the existing room-wide discussion, including recipient selection, privacy boundaries, cooldowns, moderation policy, and scalable event/data design. Replaced the room-wide interface with recipient-only tagged messages: `@recipient message` is persisted and included only in sender/recipient snapshots; active-player validation, Ghost restriction, sender-visible identity, 3-second server cooldown, and focused parser/privacy regressions are in place.
- [x] Add a Skip Vote option with clear eligibility, result handling, tie interaction, abstention rules, and UI feedback. Active players can submit one public Skip Vote; it is distinct from host-recorded offline abstention, locks the ballot, appears in the live feed, participates in completion count, and returns to clue play without elimination if every player skips.
- [x] Redesign entry and host identity flows: offer Create Room and Join Room choices, accept Room ID/password for joining, and give the room creator a clear Host/Player mode toggle so the host can also participate without compromising host controls. Added professional Create, Join, and Recover flows; named creators receive a real player seat plus host token; server-side view switching returns player-safe snapshots when in Play view and preserves the host authority separately. A new isolated Socket.IO regression proves creator mode-switching, ready/hint participation, hidden-role privacy, and recovery with the same player seat. On Windows, that process-spawning check is intentionally executed through `npm run test:host-player` after the regular Vitest suite to keep the worker lifecycle reliable.
- [x] Replace room-wide group discussion with tagged private direct messages: `@recipient message` must be delivered only to the named eligible room participant, and the recipient must see the sender identity. Removed the discussion tabs, scores, game log, pack actions, room-wide composer, host moderation controls, and public message history from the activity panel; it is now a compact private inbox with recipient chips and a tagged composer.
- [x] Apply a modern responsive UI refresh to the approved entry, results, voting, and private-message surfaces, preserving accessibility, mobile usability, privacy states, and existing game rules. Added a cohesive professional Create/Join/Recover entry card, host/player mode controls, responsive private-message and score surfaces, and Skip Vote controls. Desktop/mobile review confirmed safe layout; all regression, audit, and production build checks passed.
- [x] Begin scalable deployment preparation only after the above game rules and interfaces are implemented, tested, and accepted. Started the non-disruptive workspace transition only after the current feature milestone’s full validation and checkpoint.
- [x] Create a provider-ready frontend/backend workspace foundation that preserves the existing local JavaScript runtime while making the web client independently buildable for a future Vercel deployment. Added independent `frontend/` and `backend/` package boundaries; the frontend build reuses the active client source during the staged migration to avoid duplicate code.
- [x] Make the Socket.IO endpoint configurable at build time without changing the current same-origin local and managed development behaviour. `VITE_REALTIME_URL` now selects an external HTTPS endpoint when configured and retains same-origin Socket.IO locally; `CORS_ORIGIN` now accepts explicit production frontend origins.
- [x] Document the explicit Supabase PostgreSQL, Redis, object-storage, environment-variable, migration, and rollout prerequisites without introducing credentials or provisioning external resources. Added provider-ready frontend/backend configuration documents and the staged migration/validation guide; no external credentials, resources, or provider connections were added.
- [x] Validate the provider-ready workspace locally and on the synchronized Windows project before requesting provider-account access for actual deployment. Sandbox passed 21 test files / 43 tests, deployment-boundary assertion, independent frontend build, UI audit, and existing production build. Windows passed 19 test files with 1 intentional platform skip, the dedicated Socket.IO creator-mode check, deployment-boundary assertion, independent frontend build, UI audit, and existing production build.
- [x] Draft and validate an idempotent PostgreSQL migration that faithfully represents the current authoritative SQLite room, role, clue, voting, private-message, score, and word-pack schema without applying it to any external project. Added `backend/migrations/001_initial_game_schema.sql`, structural Vitest coverage, and `npm run validate:postgres-schema`; sandbox and Windows validation passed without modifying any Supabase project.
- [x] Make the frontend workspace self-contained for Vercel’s configured root-directory build, including its required client dependencies and single-page-app routing fallback, without deploying it. Added a Vite SPA `vercel.json`, declared browser/build dependencies in `frontend/package.json`, and added structural regression coverage. Sandbox and Windows test suites, deployment-boundary validation, schema validation, and independent frontend builds passed.
- [x] Obtain and present the exact cost for a new dedicated Word Impostor Supabase project in the user’s Mumbai organization, then create it only after the user confirms the quoted charge. The quote was $0/month; after user confirmation, created `word-impostor` in `ap-south-1` (project `buzrzcalyozuwsxwynmv`) without purchasing a plan, domain, or add-on.
- [x] Confirm and connect the Redis provider required for multi-instance Socket.IO coordination before deploying the realtime backend. Created `word-impostor-realtime` on Upstash Free Tier in Mumbai (`ap-south-1`) with TLS. A server-only smoke check successfully connected the Supabase Data API and Redis Socket.IO adapter without printing credentials.
- [ ] Store the user-supplied Supabase server credential and Upstash TLS Redis URL only in managed server-side secrets, validate their presence without printing values, and rotate them after deployment verification because they were initially pasted into chat.
- [ ] Keep the newly created named Supabase replacement key inactive until the user explicitly authorizes its Render activation, Redis password rotation, and irreversible revocation of the prior credentials.
- [x] Implement and test a Supabase Data API repository plus Redis Socket.IO adapter behind an explicit runtime flag, retaining the existing SQLite repository as the default fallback until cloud-mode flow coverage passes. The fail-closed factory, server-only repository, optional adapter, cloud transaction checks, and cloud-mode Socket.IO lifecycle are validated; SQLite remains selected unless cloud mode is explicitly enabled.
- [x] Map and migrate the authoritative room, player, round, clue, voting, private-message, scoring, and word-pack service operations to the tested Supabase repository in reversible stages, with parity flow checks before enabling cloud gameplay. The final dispatch audit found and corrected the remaining host random-word and restart paths; the deployed Render lifecycle now exercises the full cloud operation set while local SQLite remains the explicit fallback.
- [x] Encapsulate direct SQLite service access behind a behavior-preserving repository contract and document each atomic cloud RPC boundary before changing the active game path. The service now depends on a dedicated local repository runtime seam; the pure adapter factory has isolated unit coverage, and 27 test files / 54 tests plus the production build passed.
- [x] Seed and verify all six built-in category word packs in Supabase before any cloud room can be created. Applied the secured cloud-foundation migration, seeded 60 entries across six category packs through the server-only RPC, and verified each pack contains 10 entries.
- [x] Add reviewed room-locking, idempotency, and transactional RPC migrations for concurrent cloud room, round, clue, vote, result, continuation, and purge operations. Applied migrations 004–020 provide the reviewed server-only transaction set; the final random-word path uses the existing locked, idempotent lobby-settings transaction and restart uses the host-token-verified purge transaction.
- [x] Repair the corrupted Windows npm installation (missing Arborist `edge.js`), install synchronized cloud-adapter dependencies, and rerun the Windows source regression/build validation. Recovered Node.js with the official per-user LTS package; Windows passed 26 test files / 53 tests (two expected cloud/host-player skips), PostgreSQL schema validation, deployment-boundary validation, production build, and the dedicated Host/Player Socket.IO check.
- [x] Run a server-only cloud-adapter smoke check against the new Supabase project and free Upstash database, ensuring secrets remain absent from test output and the existing SQLite gameplay path is unaffected. `npm run test:cloud-adapters` reports `CLOUD_ADAPTER_CHECK supabase=ok redis=ok secrets=redacted`.
- [x] Add validated backend runtime configuration for private PostgreSQL and Redis environment variables, preserving the current SQLite single-instance path until both external services are explicitly configured and tested. Added a tested server-only `WORD_IMPOSTOR_POSTGRES_URL`/`UPSTASH_REDIS_URL` readiness boundary, avoiding unrelated platform `DATABASE_URL` values. `/api/health` confirms the running local path is SQLite with Redis disabled and exposes no secrets. Full tests, schema check, boundary check, build, and UI audit passed.
- [x] Apply the validated PostgreSQL schema migration to the new dedicated Word Impostor Supabase project and verify the required tables, indexes, and advisor status. All 15 authoritative tables were created. Migration history records the initial schema, backend-only RLS, and foreign-key index changes. The post-migration advisor contains only expected INFO-level notices for zero-use indexes on an empty database; no foreign-key coverage warnings remain.
- [x] Add and apply a backend-only Supabase Row Level Security migration before any client or service key is configured, then verify the critical exposure advisory is resolved. RLS is enabled on all 15 tables and anon/authenticated table access is revoked. The former critical RLS-disabled warning is resolved; the remaining INFO notices report the intentional no-policy backend-only design.
- [x] Add and apply targeted PostgreSQL indexes for the foreign-key relationships identified by the Supabase performance advisor, then recheck the database findings. Added and verified 15 targeted foreign-key indexes; the refreshed performance advisor reports no unindexed-foreign-key findings.
- [x] Complete the asynchronous game-service repository switch and prove SQLite/Supabase behavior parity without enabling cloud mode by default. The async dispatcher now covers every persistence-changing Socket.IO game command, and local plus public cloud lifecycle checks passed while cloud mode remains explicit and SQLite remains default.
- [x] Select and configure a verified no-cost WebSocket-compatible backend host, documenting its sleep, usage, and uptime limitations before deployment. Render Free staging is deployed in Singapore and uses the public Node/Socket.IO backend origin documented in `docs/realtime-backend-host-options.md`.
- [x] Deploy the isolated frontend workspace to a new Vercel project and configure it only with the public realtime backend origin. The `word-impostor` Vite project is live at `word-impostor-ten.vercel.app`; its compiled public bundle contains the Render realtime origin and no Supabase or Redis provider identifier.
- [x] Add and validate a Vercel-only Vite build configuration that outputs inside the `frontend/` deployment root while preserving the existing root `dist/public` output required by the managed combined deployment. `frontend/vite.vercel.config.js` is the default isolated frontend build, while root `npm run build` retains its combined managed output.
- [x] Validate a multi-client production room through direct-message privacy, clues, voting, scoring, continuation, host recovery, and end-room cleanup. After the CORS deployment, the redacted public lifecycle again passed every stated stage; a Vercel-browser-created disposable room also accepted a second live Socket.IO participant and rejected rejoin after host termination.
- [ ] Rotate the Supabase server credential and Upstash Redis credential that were pasted into chat, update managed server-only secrets, and rerun all cloud smoke checks.
- [x] Apply and verify the approved server-only player-presence RPC, then remove direct cloud-flow connection-state updates. Migration 014 is applied; the disposable cloud flow proves idempotent presence, readiness, host kick, full gameplay lifecycle, and cleanup without direct connection-state updates or secret output.
- [x] Add and test a server-only cloud snapshot model that filters roles, secret words, host controls, and direct messages by verified audience. Pure privacy tests and the live disposable cloud lifecycle verify player-safe roles, host-only pack choices and peek mode, recipient-only direct-message reads, and redacted event metadata.
- [x] Add and test server-only cloud lookup methods required for password-verified host recovery and player-session reconnect dispatch. The cloud flow proves room-code and host-token recovery lookup plus creator and session-token player lookups without logging returned credential values.
- [x] Add a fail-closed asynchronous command dispatcher and cloud snapshot emitter while preserving synchronous SQLite behavior until every Socket.IO event reaches parity. Final audit and regression coverage confirm host randomization and restart are dispatched through the same cloud-aware boundary as all other persistence commands, with audience-filtered cloud snapshots and unchanged SQLite fallback behavior.
- [x] Draft, apply, and validate the server-only host-control RPC bundle for settings, player order, turn progression, discussion lock, and manual role reveal. Migration 015 is applied; the cloud flow validates each transaction, idempotency where applicable, phase protections, role reveal, and cleanup.
- [x] Complete the full backend parity and staging-deployment validation suite, documenting verified residual platform limitations rather than suppressing warnings. The live Render Free lifecycle validates cloud mode, Redis adapter attachment, privacy-safe snapshots, packs, direct messages, alerts, moderation, voting, scores, continuation, leave, host recovery, and full cleanup; the residual limits remain idle sleep, cold starts, single instance, and ephemeral disk.
- [x] Make the deployed realtime backend Supabase-database-backed; retain SQLite only as a local development rollback until cloud-mode staging parity is proven. The approved Render Free staging service runs with explicit cloud mode and server-only Supabase/Redis configuration; its public lifecycle passed while local runtime defaults remain fail-closed SQLite.
- [x] Add a tested asynchronous Supabase service for Create Room, Join/Reclaim, Host Recovery, and audience-filtered snapshots before routing those Socket.IO events in cloud mode. The fail-closed dispatcher and Socket.IO entry handlers now use it only when cloud selection is explicit; focused service and dispatcher tests pass.
- [x] Add and run an isolated cloud-mode Socket.IO entry check for Create, Join/Reclaim, Host Recovery, presence, and privacy-safe snapshots before enabling cloud gameplay. The disposable Supabase-backed Socket.IO check validates host-only versus player-safe snapshots, reconnect presence, recovery, and complete cleanup with redacted output.
- [x] Draft, apply, and live-validate server-only cloud lifecycle transactions for host-controlled word-pack management. Migration 017 adds custom upload, per-room enablement, and deletion; the full cloud Socket.IO lifecycle verifies host-only visibility and round-safe behavior.
- [x] Apply and verify the narrow server-only foreign-key correction for custom word-pack deletion after the live cloud flow exposed the protected room-pack link. Migration 018 removes room-pack links before the parent custom pack and the full lifecycle now completes with cleanup.
- [x] Draft, apply, and live-validate server-only cloud transactions for disconnected-voter abstention, host clue moderation, discussion moderation, and room alerts. Migration 019 introduced the controls; migration 020 corrected its ballot-stage key. The redacted cloud Socket.IO lifecycle now validates nudge and flash delivery, discussion removal, clue editing, an offline-voter tally, recap awards, continuation, rejoin, leave, host recovery, and full cleanup.
- [x] Apply and live-validate the server-only voluntary player-leave transaction. Migration 016 is applied; the disposable cloud Socket.IO lifecycle proves a departing player becomes a Ghost, roster privacy remains intact, the room continues when valid, and the session purges cleanly.
- [x] Reorganize the repository into clear frontend and backend boundaries, remove only verified unused or generated files, update all references, and validate sandbox plus Windows workflows. Canonical UI source now lives in `frontend/`; Socket.IO server, migrations, scripts, shared helpers, built-in packs, and local rollback data live in `backend/`. Removed obsolete root folders, generated archives/output/metadata, legacy backup data, and non-runtime template files while retaining tests and migration history. Sandbox passed 31 files / 64 tests plus cloud and build gates; Windows passed 29 files / 62 tests with two intentional cloud/host-player skips, architecture/UI/Host-Player/schema checks, and build.
- [x] Create the user-approved Render Free staging backend from the isolated backend workspace, configuring only server-side Supabase/Redis/cloud-mode settings and preserving the local SQLite fallback. The service deploys from the verified `main` branch without any secret committed to Git.
- [x] Validate the public staging backend with health, Socket.IO, audience-privacy, moderation, alerts, and cleanup checks; document the 15-minute idle sleep, cold-start, single-instance, and ephemeral-filesystem limitations. The final redacted public lifecycle passed against the live service and confirms explicit cloud mode plus the Redis Socket.IO adapter.
- [x] Prevent the backend-only Render service from attempting to serve an absent frontend bundle at `/`, while retaining root `dist/public` SPA serving for managed combined deployments. The latest Render revision returns an intentional application JSON 404 at `/` and HTTP 200 at `/api/health`; JavaScript unit coverage preserves both backend-only and combined-deployment behavior.
- [x] Restrict the deployed Render Socket.IO CORS policy to the intentional Vercel production origin and verify it after redeployment. `CORS_ORIGIN` allows `https://word-impostor-ten.vercel.app`; an approved-origin handshake returns that allow-origin header, an unapproved origin receives no allow-origin header, the deployed Vercel browser successfully creates a realtime room, and the non-secret operating guide records the final boundary.
- [x] Audit and correct every active Word Impostor UI page for small phones, tablets, laptops, large desktops, and TV-scale screens: no horizontal clipping, overlapping controls, unreadable text, unreachable actions, or unsafe fixed panels; preserve the game’s existing JavaScript/JSX logic, privacy rules, and accessibility behavior. Added a centralized responsive layer covering compact phones, tablet portrait, landscape screens, wide desktops, and TV-scale displays; corrected the 768px entry overlap and Create Room note wrapping; hardened safe-area fixed controls, top-bar overflow, activity-panel height, host-grid reflow, and wide-screen content scale. Phone, tablet, laptop, and TV captures plus active host/player/game/private-message checks passed; 35 test files / 102 tests, UI audit, schema, deployment boundary, managed and Vercel builds, and Host/Player validation passed.
